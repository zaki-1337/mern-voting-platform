import React from "react";
import ReactDOM from "react-dom/client";
import App from "./Components/App";

const rootM = ReactDOM.createRoot(document.getElementById("root"));
rootM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
